# NixOS Config
_My personal configuration for NixOS_

## Getting Started
1. Create a `secrets.nix` file at the `/etc/nixos` directory
2. Make sure the file has the following variables:
  - `password`: The password for the main user
